import blockContent from './schemas/blockContent'
import category from './schemas/category'
import event from './schemas/event'
import author from './schemas/author'

export const schema = {
  types: [event, author, category, blockContent],
}
